import '../styles'
import { ThreadBody } from '../components/thread/Detail'

export default {
  title: 'Thread Body',
  component: ThreadBody
}

export const Default = {
  args: {
    title: 'Example Thread',
    category: 'Fun',
    body: 'body of example thread'
  }
}
